# Filesystem
alias ls='ls --color=auto'
alias ll='ls -l'
alias la='ls -la'

# Sudo
alias sz='sudo -E zsh'

# Grep
alias grep='grep --color=auto'

# Tmux
alias ta='tmux new-session -A -s'
alias t='ta main'

# Neovim
alias vim="nvim"

# Kubernetes
alias k="kubectl"
