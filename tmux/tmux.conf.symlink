# Load all configuration files
run 'for config in $DOTFILES/tmux/*.conf; do [ "$config" != "$DOTFILES/tmux/plugins.conf" ] && tmux source-file $config; done'

# Plugins must be loaded last
run 'tmux source-file $DOTFILES/tmux/plugins.conf'
